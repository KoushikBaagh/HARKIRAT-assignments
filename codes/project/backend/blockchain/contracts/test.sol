// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract calc {
    uint num =0;
    constructor(uint _num)
    {
        num = _num; // _num is the initail value
        // num = _initialValue; 
    }
}